# Backend confiugration
tenant_id               = "#{tenant_id}"
subscription_id         = "#{subscription_id}"
storage_account_name    = "#{storage_account_name}"
container_name          = "#{container_name}"
access_key              = "#{access_key}"
